server:
  port: 9001

spring:
  boot:
    admin:
      client:
        url: http://localhost:9000

management:
  endpoint:
    health:
      show-details: always

  endpoints:
    web:
      exposure:
        include: "*"
